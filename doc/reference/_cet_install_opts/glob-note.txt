      .. note::

         The use of :command:`file(GLOB)
         <cmake-ref-current:command:file(glob)>` expressions in
         CMake build/install lists is discouraged due to its tendency to
         create hysteresis.
